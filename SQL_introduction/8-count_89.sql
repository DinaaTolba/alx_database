-- Script to count the number of records with id = 89 in the table 'first_table' of the database 'hbtn_0c_0'

-- Query to count the records
SELECT COUNT(*) AS count_89
FROM first_table
WHERE id = 89;
