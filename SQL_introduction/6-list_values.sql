-- Script to list all rows of the table 'first_table' from the database 'hbtn_0c_0'

-- Check if the database exists
USE hbtn_0c_0;

-- Query to select all fields from the table 'first_table'
SELECT id, name
FROM first_table;
